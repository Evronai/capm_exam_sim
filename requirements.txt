# Core Streamlit and UI
streamlit==1.28.1
pillow==10.1.0  # For image handling

# Data manipulation and analysis (helpful for future expansions)
pandas==2.1.3
numpy==1.24.3

# For any plotting or visualizations (performance charts)
plotly==5.18.0
matplotlib==3.8.2

# For session state management and advanced features
extra-streamlit-components==0.1.8

# Optional but recommended for better performance
watchdog==3.0.0  # For file system watching during development

# For PDF report generation (if you want to add this feature later)
reportlab==4.0.9
fpdf==1.7.2

# For data storage (if you want to track user progress)
sqlalchemy==2.0.23
sqlite3  # Built-in, but listed for reference

# For any API integrations or web requests
requests==2.31.0

# For date/time handling
python-dateutil==2.8.2
pytz==2023.3

# For progress bars and visual feedback
tqdm==4.66.1

# For generating unique IDs (if needed for user tracking)
uuid  # Built-in, but listed for reference

# For data validation
pydantic==2.5.0

# For environment variable management (if deploying)
python-dotenv==1.0.0

# Testing dependencies (optional, for development)
pytest==7.4.3
pytest-cov==4.1.0
